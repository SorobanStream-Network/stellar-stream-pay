{
  "name": "stellar-stream-pay-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Express indexer & API for StellarStream-Pay — reads stream state from Soroban RPC and account balances from Horizon.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^16.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
