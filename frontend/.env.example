# Copy to .env and adjust. Testnet defaults shown.
VITE_CONTRACT_ID=            # deployed StreamingContract id (C...) — REQUIRED
VITE_RPC_URL=https://soroban-testnet.stellar.org
VITE_NETWORK_PASSPHRASE=Test SDF Network ; September 2015
VITE_BACKEND_URL=http://localhost:4000
VITE_HORIZON_URL=https://horizon-testnet.stellar.org   # pre-flight balance/trustline checks
VITE_MIN_GAS_XLM=1           # coarse XLM gas floor checked before the wallet dialog
